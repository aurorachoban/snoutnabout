"use client";

import { Icon } from "@iconify/react";

export function Dog({ className }) {
  return <Icon icon="mdi:dog" className={className} />;
}

export function Cat({ className }) {
  return <Icon icon="mdi:cat" className={className} />;
}

export function Truck({ className }) {
  return <Icon icon="mdi:truck-outline" className={className} />;
}

export function Heart({ className }) {
  return <Icon icon="mdi:heart-outline" className={className} />;
}

export function RotateCcw({ className }) {
  return <Icon icon="mdi:rotate-left" className={className} />;
}

export function PawPrint({ className }) {
  return <Icon icon="mdi:paw" className={className} />;
}
