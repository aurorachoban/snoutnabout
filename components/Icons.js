"use client";

// Re-exports lucide-react icons as client components so they can be safely
// used inside server components (page.js) without a render error.
export { Dog, Cat, Truck, Heart, RotateCcw, PawPrint } from "lucide-react";
